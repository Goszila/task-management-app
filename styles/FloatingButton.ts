import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    position: 'absolute',
    right: 20,
    bottom: 20,
    backgroundColor: '#007bff',
    borderRadius: 50,
    padding: 16,
  },
});